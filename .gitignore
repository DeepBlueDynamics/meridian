# ── Secrets / env — never commit real keys ──────────────────────────────
.env
.env.*
*.env
!.env.example

# ── Boat & map data (local datasets, not reviewable code) ────────────────
enc_charts/
crawl_cache/
sailboat_data/
cruiser_boats.json
isolate_cruisers.py
*.pmtiles
charts/

# ── Logs / telemetry ──────────────────────────────────────────────────────
logs
*.log
telemetry.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Build / deps / bundles ────────────────────────────────────────────────
node_modules
dist
dist-ssr
*.local
meridian-review-*.zip

# ── Tooling cruft ─────────────────────────────────────────────────────────
.mcp-logs/
.codex-mcp.config
.codex-container.toml

# ── Editor directories and files ──────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Spec management (junction + consumed drops stay local) ──────────────
spec/current
spec/archive/
